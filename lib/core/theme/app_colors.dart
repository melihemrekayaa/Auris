import 'package:flutter/material.dart';

class AppColors {
  // Backgrounds
  static const Color backgroundDark = Color(0xFF0F172A); // Slate 900
  static const Color backgroundDarker = Color(0xFF020617); // Slate 950
  static const Color backgroundLight = Color(0xFF1E293B); // Slate 800

  // Accents
  static const Color primary = Color(0xFF9D4EDD);
  static const Color primaryDark = Color(0xFF5A189A);
  static const Color secondary = Colors.purpleAccent;
  static const Color tertiary = Colors.blueAccent;

  // Text
  static const Color textPrimary = Colors.white;
  static const Color textSecondary = Colors.white70;

  // Glassmorphism
  static Color glassBackground = Colors.white.withOpacity(0.03);
  static Color glassBorder = Colors.white.withOpacity(0.1);
}
